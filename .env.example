# WordPress site URL (used for image/asset URLs only — NOT for API fetch paths).
# API requests always use same-origin /wp-json/* proxies (Vite dev proxy or Vercel rewrites).
# Set this in Vercel → Project Settings → Environment Variables for Production.
VITE_WORDPRESS_URL=https://sassystrides.com
